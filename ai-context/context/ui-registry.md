# UI Registry

> **Living document.** `designed` = proven in the design prototype, not yet built in React. `built` = shipped, and its classes below are the real ones.
> Read before building any component. Flip a row to `built` the moment it lands, in the same session.

Catalogs every component, where it lives, and the classes it uses. The styling contract and tokens live in [ui-rules.md](ui-rules.md).

---

## How to use

**Before building:**

1. Look for a component that already does this.
2. **Exists** → reuse it. Match its classes exactly. Do not create a near-duplicate under a new name.
3. **Close but not exact** → add a variant or a prop to the existing one. Prefer a variant over a new component.
4. **Nothing fits** → build it per [ui-rules.md](ui-rules.md) §5, then add a row here.

**After building:** add the row (real path, exact classes, `built`) and a change-log entry the same session. An unregistered component gets duplicated by the next one.

**Placement** ([architecture.md](architecture.md) §3–4):

| It is… | It goes in |
|---|---|
| Generic, knows nothing about projects/jobs/skills | `components/ui/` |
| Site chrome present on every page | `components/layout/` |
| Presents a domain concept | `components/sections/` |

`components/ui/` importing from `content/` is a lint error, not a style preference.

**Status:** `planned` (named only) · `designed` (exists and verified in the design prototype) · `built` (implemented in React, in the repo)

**The prototype is the visual contract.** Every `designed` row has working markup, both themes, and all its states already proven. When building, match it — do not redesign in React.

---

## 1. Foundations

| Item | Source |
|---|---|
| Color, type, spacing, radius, motion tokens | [ui-rules.md](ui-rules.md) §3 |
| `cn()` class merger | `src/lib/utils.ts` |
| Breakpoints (1000 / 760 / 460) | [ui-rules.md](ui-rules.md) §4 |
| Design prototype (the visual contract) | published Artifact — all 9 pages, both themes |

---

## 2. `components/ui/` — primitives

All `designed` — they exist and are proven in the design prototype, not yet built in React.

| Component | Status | File | Props / variants | Prototype class |
|---|---|---|---|---|
| `Button` | **built** | `src/components/ui/button.tsx` | `variant: primary|outline|ghost` (default `primary`), `size: sm|md` (default `md`), `className`; a discriminated union on `href` — with it, every `next/link` prop; without it, every `<button>` prop | base `inline-flex cursor-pointer items-center justify-center gap-2 whitespace-nowrap` · `rounded-md text-sm font-medium transition-colors` · `focus-visible:ring-ring focus-visible:ring-offset-ground focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:outline-none` · `active:brightness-95` · `[&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0` · `disabled:pointer-events-none disabled:opacity-50` — variants `primary` `bg-fern text-fern-on hover:bg-fern-hover` / `outline` `border-border text-ink hover:bg-surface-2 border` / `ghost` `text-muted hover:text-ink hover:bg-surface-2` — sizes `sm` `h-9 px-3` / `md` `h-10 px-4` |
| `Card` | **built** | `src/components/ui/card.tsx` | `padded?: boolean` (adds `p-6`), `className`, plus every native `<div>` prop via `ComponentPropsWithoutRef<"div">` | `rounded-lg border border-border bg-surface` at rest, `p-6` added when `padded`. No shadow, no hover state — a plain box. Elevation and interaction belong to whatever wraps it. |
| `Badge` | **built** | `src/components/ui/badge.tsx` | `className`, plus every native `<span>` prop via `ComponentPropsWithoutRef<"span">` — no `tone` prop | `inline-flex items-center rounded-full border border-border px-2.5 py-0.5 text-badge font-mono text-muted`. One style only — the prototype's `neutral\|fern\|coral` tones are real design intent but not built; see the PORT-022 change-log entry for why. |
| `Chip` | **built** | `src/components/ui/chip.tsx` | `pressed: boolean`, `className`, plus every native `<button>` prop except `type` (forced `"button"`) via `Omit<ComponentPropsWithoutRef<"button">, "className" \| "type">` | `inline-flex cursor-pointer items-center rounded-full border px-3 py-1.5 text-sm font-medium transition-colors` + focus ring — `pressed` swaps `border-fern-wash bg-fern-wash text-fern` for idle's `border-border text-muted hover:border-fern hover:bg-surface-2 hover:text-ink`. Sets `aria-pressed` for real toggle semantics, not just a color change. Built inside PORT-031 for `ProjectFilter`; unmeasured against the prototype (still not in the repo), same caveat as `Cta`'s panel. |
| `Icon` | **built** | `src/components/ui/icon.tsx` | `name: IconName` (12 names, `src/lib/icons.ts`), plus every prop the underlying lucide-react component takes, via `ComponentProps<(typeof ICONS)[IconName]>` | No class of its own — renders the picked lucide component directly, defaulting `size={18} strokeWidth={1.9}` (ui-rules §3). Decorative by default: lucide-react sets `aria-hidden="true"` itself unless an accessibility prop is passed. |
| `Input` | **built** | `src/components/ui/input.tsx` | every native `<input>` prop via `ComponentPropsWithoutRef<"input">`, plus `className` | `w-full rounded-md border border-border-strong bg-ground px-3 py-2 text-base text-ink placeholder:text-faint` + focus ring + `aria-invalid:border-coral` |
| `Textarea` | **built** | `src/components/ui/textarea.tsx` | every native `<textarea>` prop via `ComponentPropsWithoutRef<"textarea">`, plus `className` | Same as `Input` plus `resize-y` — vertical only, never horizontal |
| `Field` | **built** | `src/components/ui/field.tsx` | `name` (required — `id` is derived from it as `` `field-${name}` ``), `label`, `hint?`, `error?`, `required?`, `className`, `children: (props: FieldRenderProps) => ReactNode` — render-prop wiring | `space-y-1.5` wrapper; label `text-sm font-medium text-ink`; required mark `text-coral-text` (not `text-coral`); hint/error `text-sm text-faint` / `text-sm text-coral-text` |
| `Note` | designed | `ui/note.tsx` | `tone: neutral|ok|bad` — inline feedback banner | `.note`, `.note--ok`, `.note--bad` |
| `Prose` | **built** | `src/components/ui/prose.tsx` | `children`, `className` | `max-w-measure text-base text-muted break-words` + descendant rules: `[&>*+*]:mt-5` · `[&_h2]:text-h-md [&_h2]:text-ink [&_h2]:mt-12` · `[&_h3]:text-h-sm [&_h3]:text-ink [&_h3]:mt-8` · `[&>h2:first-child]:mt-0 [&>h3:first-child]:mt-0` · `[&_strong]:text-ink [&_strong]:font-semibold` · `[&_em]:italic` · `[&_a]:text-fern [&_a]:font-medium [&_a]:underline [&_a]:underline-offset-4 [&_a]:transition-colors [&_a:hover]:text-fern-hover` · `[&_ul]:list-disc [&_ol]:list-decimal [&_ul]:pl-5 [&_ol]:pl-5` · `[&_li]:mt-2 [&_li]:marker:text-faint` · `[&_code]:bg-surface-2 [&_code]:text-ink [&_code]:rounded-sm [&_code]:px-1.5 [&_code]:py-0.5 [&_code]:font-mono [&_code]:text-sm` · `[&_blockquote]:border-border-strong [&_blockquote]:border-l-2 [&_blockquote]:pl-4` · `[&_hr]:border-border [&_hr]:my-10` |

---

## 3. `components/layout/` — site chrome

| Component | Status | File | Client? | Prototype class |
|---|---|---|---|---|
| `Container` | **built** | `src/components/layout/container.tsx` | no | `mx-auto w-full max-w-shell px-gut` |
| `Section` | **built** | `src/components/layout/section.tsx` | no | `py-section` / `py-section-tight`; heading is `text-h-md text-ink mb-6`. Props: `heading?`, `id?`, `spacing?: "default" | "tight"`, `bleed?`, `className?`. Wraps children in `Container` unless `bleed`. |
| `SectionHead` | designed | `layout/section-head.tsx` | no | `.sec-head` + `.rule` — eyebrow, heading, gradient rule |
| `Header` | **built** | `src/components/layout/header.tsx` | no | `bg-ground/80 border-border sticky top-0 z-40 border-b backdrop-blur-md`; inner `Container` is `flex h-16 items-center justify-between gap-4`; wordmark `text-h-sm text-ink -mx-2 rounded-md px-2 py-1 transition-colors hover:text-fern` + focus ring. Nav wrapper `hidden lg:block`. Props: `className?`. Holds the hardcoded `NAV` array until PORT-011. |
| `NavLinks` | **built** | `src/components/layout/nav-links.tsx` | **yes** — `usePathname` | `ul` is `flex items-center gap-1` (horizontal) or `flex flex-col items-stretch gap-1` (vertical); link `block rounded-full transition-colors` + `px-3 py-1.5 text-sm` / `px-4 py-3 text-base` + focus ring; active `bg-fern-wash text-fern font-medium`, idle `text-muted hover:text-ink hover:bg-surface-2`. Props: `items: readonly NavItem[]`, `orientation?: "horizontal" \| "vertical"` (default horizontal), `onNavigate?: () => void`, `className?`. Exports the `NavItem` type. |
| `MobileMenu` | **built** | `src/components/layout/mobile-menu.tsx` | **yes** — focus trap, Escape, scroll lock | Burger: `inline-flex size-9 shrink-0 items-center justify-center rounded-full` + `border-border text-muted cursor-pointer border transition-colors hover:bg-surface-2 hover:text-ink` + focus ring. Portal wrapper: `fixed inset-x-0 top-16 bottom-0 z-50 flex flex-col overflow-hidden`. Sheet: `bg-surface border-border animate-sheet-in max-h-full overflow-y-auto overscroll-contain border-b`, inner `nav` is `px-gut py-4`. Scrim: `bg-ground/60 animate-scrim-in flex-1 backdrop-blur-sm`. Props: `items: readonly NavItem[]`, `className?` (lands on the burger). |
| `Footer` | **built** | `src/components/layout/footer.tsx` | no | `border-border mt-auto border-t`; inner `Container` is `flex flex-col gap-4 py-8 md:flex-row md:items-center md:justify-between`; copyright `text-faint text-badge font-mono`; links `text-muted hover:text-fern inline-flex items-center gap-1 rounded-md text-sm transition-colors` + focus ring, each with an `ArrowUpRight` at 14. Props: `className?`. Needs `<body>` to be `flex min-h-full flex-col` for `mt-auto`. |
| `SkipLink` | **built** | `src/components/layout/skip-link.tsx` | no | `sr-only` + `focus:not-sr-only focus:fixed focus:top-4 focus:left-4 focus:z-50` + `focus:bg-fern focus:text-fern-on focus:rounded-md focus:px-4 focus:py-2 focus:text-sm focus:font-medium` + `focus:ring-*`. Props: `className?`. Targets `#main`; must be the first child of `<body>`. |
| `ThemeToggle` | **built** | `src/components/layout/theme-toggle.tsx` | **yes** — `next-themes`, nothing rendered before hydration | `inline-flex size-9 shrink-0 items-center justify-center rounded-full` + `border-border text-muted cursor-pointer border transition-colors hover:bg-surface-2 hover:text-ink` + focus ring. Props: `className?`. Pre-hydration it renders the same-sized empty `div`, gated by `useSyncExternalStore`, **not** `useState`+`useEffect` — React Compiler lint rejects setState in an effect. |

---

## 4. `components/sections/` — domain composites

| Component | Status | File | Client? | Used on |
|---|---|---|---|---|
| `Hero` | **built** | `src/components/sections/hero.tsx` | no | `/`. Props: `className?`. Reads `site` directly (the Header/Footer carve-out, content-model.md §4) for role/name/tagline — no props for that content. Eyebrow `text-eyebrow text-faint mb-4 font-mono uppercase`, name `text-h-xl text-ink`, tagline `text-lead text-muted mt-6 max-w-measure`. No buttons — PORT-030 put both CTAs in the closing `Cta` band instead. |
| `StatusPill` | designed | `sections/status-pill.tsx` | no | `/` — `.status` + animated `.dot`. **Not built by PORT-030** — no source copy (status text) exists yet and the prototype it's drawn from isn't in the repo; see progress.md decisions log 2026-08-24. |
| `StatRow` | designed | `sections/stat-row.tsx` | no | `/` — `.stat-row`. **Not built by PORT-030**, same reason as `StatusPill` — no stat numbers exist as content yet. |
| `ProjectCard` | **built** | `src/components/sections/project-card.tsx` | no | `/`, `/projects`. Props: `project: Project`, `className?`. Whole card is a `next/link` `aria-label`led to the project title; `Card` sits inside it with `overflow-hidden p-0` and `group-hover:`/`group-focus-visible:` border + `shadow-card-lift`. Thumbnail is `next/image fill` inside an `aspect-thumbnail` box. Tags render as `<li><Badge>{tag}</Badge></li>` — swept from the inline chip classes in PORT-022. |
| `ProjectGrid` | **built** | `src/components/sections/project-grid.tsx` | no | `/projects`. Props: `projects: Project[]`, `emptyState?: ReactNode`, `className?`. `grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3` — 1 col below `md`, 2 at `md:760`, 3 at `lg:1000`, matching PORT-031's AC exactly. Renders `emptyState` (or a plain fallback paragraph) instead of the grid when `projects` is empty; knows nothing about *why* it's empty — that's the caller's copy. **Not yet wired into `/`** — Home's featured grid (PORT-030) still inlines its own `div` at a different breakpoint (`sm:grid-cols-2 lg:grid-cols-3`); see progress.md's 2026-08-24 decisions log. |
| `ProjectFilter` | **built** | `src/components/sections/project-filter.tsx` | **yes** — URL `?tag=` via `router.push` | `/projects`. Props: `tags: string[]`, `activeTag?: string`, `className?`. `<ul aria-label="Filter projects by tag">` of `<li><Chip></li>`, "All" first. Takes `activeTag` as a **prop** from the server page rather than calling `useSearchParams()` itself — sidesteps that hook's `<Suspense>`-wrapping requirement, keeping this the only Client Component the ticket needed. Writes the URL; never filters — the page re-renders server-side from the same `searchParams` it already reads. |
| `SkillTier` | **built** | `src/components/sections/skill-tier.tsx` | no | `/skills`. Props: `group: SkillGroup`, `className?`. Takes the whole `SkillGroup` because every field on the type reaches the screen. Wrapper `flex flex-col`; the meter is a **fixed-height** `flex h-2.5 items-center gap-1.5` row of three `<span>`s, then `h3` `text-h-sm text-ink mt-4`, blurb `text-muted mt-2 text-sm`, items as `<li><Badge>` in `mt-5 flex flex-wrap gap-2`. **Depth is encoded by filled count AND dot size, never by colour strength** — `TIER_WEIGHT` fills 3/2/1 and `TIER_DOT_SIZE` steps `size-2.5`/`size-2`/`size-1.5`, every filled dot full-strength `bg-fern`; unfilled are `border-border bg-transparent`. Both are full `Record<SkillTier, …>`, so a fourth tier fails the build rather than rendering a meter that reads as "none". **The registry's original `.tier-dot` treatment — saturation — was measured and dropped:** wash tokens came out at 1.02–1.23:1 against the page ground and `fern` at 70%/45% opacity at 2.79:1/1.86:1 in light, all under WCAG 1.4.11's 3:1, because `--fern` is only 4.78:1 to start with. Size carries the same information at zero contrast cost. **`h-2.5` on the meter row is load-bearing:** different dot sizes centre differently, and without a fixed height the three tier headings stop aligning across the grid row. The meter is `aria-hidden` — `blurb` already states the tier's meaning in words, so exposing three decorative dots would announce the same fact twice, less precisely. |
| `StackStrip` | designed | `sections/stack-strip.tsx` | no | `/`, `/skills` — `.stack-item`. **Not built by PORT-030**, same reason as `StatusPill` — no stack-item content source decided yet. |
| `PracticeCard` | **built** | `src/components/sections/practice-card.tsx` | no | `/skills`. Props: `practice: Practice`, `className?`. Takes a `Practice` rather than three loose values — unlike `CaseStudy`, which takes plain strings because "heading then prose" is a shape anything can reuse, this renders one specific content type and nothing else has that shape. A `Card padded` with `h-full`, an `<Icon size={20}>` inside a `border-border bg-surface-2 text-fern flex size-10 items-center justify-center rounded-lg border` tile, then `h3` `text-h-sm text-ink mt-5` and body `text-muted mt-3 text-sm`. **`h-full` is load-bearing in the grid** — without it a card with a shorter body leaves a ragged bottom edge against the tallest card in its row. The icon sits in a bordered tile rather than floating loose so a two-line heading cannot pull the mark out of alignment with its neighbours. First real use of four of the eight `Practice` icon names PORT-024 reserved; the other four remain available. |
| `Timeline` | **built** | `src/components/sections/timeline.tsx` | no | `/resume`. Props: `entries: TimelineEntry[]`, `icon: IconName`, `emptyState: ReactNode`, `className?`. **Takes `TimelineEntry`, deliberately NOT `Job` or `Education`** — both feed it and differ only in field names (company/institution, role/credential), so the page maps each into a neutral shape and one component serves both lists rather than a union with a discriminant. Wrapper `<ol class="space-y-10">`; each `<li>` is `relative pl-10 break-inside-avoid`. **The rail is a `::before` pseudo-element on each item** (`before:bg-border before:absolute before:top-0 before:bottom-0 before:left-[11px] before:w-px`), NOT one absolutely positioned line down the list — an absolute rail takes its height from the screen layout and runs off the bottom of a printed page. `last:before:bottom-auto last:before:h-6` trims the final item's line to its marker so it stops at the last node. Marker is `bg-ground absolute top-0 left-0 flex size-6 items-center justify-center rounded-full` carrying an `<Icon size={13}>`, `aria-hidden`, filled with the page ground so the rail does not show through; a current entry (`end === null`) takes `text-fern ring-fern ring-2`, all others `text-faint ring-border ring-1`. **The title/date row is `md:flex md:items-baseline md:justify-between md:gap-x-6` — breakpoint-driven, never `flex-wrap`.** The first build used `flex flex-wrap` and measured clean, but looked broken: at 460 and 375 the two long titles pushed their dates to a second line while the short "Web Developer" kept its date beside it, so one list rendered in two layouts at the same width. A wrap decision is made per item by string length; a breakpoint decision is made once for every item. Date is `md:shrink-0` so the range never breaks mid-value once it is a row. Bullets are a `<ul>` whose markers are `before:content-['—']`, not `list-disc`. **The `end: null` / current-entry ring is unexercised by real content** — no job in `experience.ts` is current. **Checked against the prototype by Vernel 2026-09-02 and confirmed to match** — worth recording because it could not be verified any other way: the registry's `.tl` / `.tl-item--now` were prototype class names that existed nowhere in `src/`, so the rail-and-node treatment was built from the description alone.
| `ContactForm` | **built** | `src/components/sections/contact-form.tsx` | **yes** — `useActionState` + `useFormStatus` | `/contact`. Props: `className?`. The one Client Component on the page — the leaf, per code-standards.md §4, so `Field`/`Input`/`Textarea` all stay Server Components exactly as PORT-023 built them. **PORT-041 wired it to the real Server Action:** `useActionState(submitContact, null)` replaced `useState`, and `mockValidate` + the `setTimeout` are gone. The union it reads is now `ActionResult` itself (`null` = never submitted, which is why there is no `idle` member), so PORT-036's promise held — no JSX branch changed for the swap. **Three structural pieces PORT-041 added.** `ContactForm` is now a thin wrapper holding a `formKey`, with the real form in `ContactFormFields`; bumping the key remounts it, which is the ONLY way to clear a `useActionState` result — the hook exposes no reset, and the first attempt (a `<Link href="/contact">`) was **dead**, because same-route navigation keeps the component mounted and the success panel never goes away. `SubmitButton` is its own component because `useFormStatus` reads the PARENT form's status and would report `pending: false` forever inside the form that renders it. `Spinner` is an inline `<svg className="animate-spin">`, `aria-hidden`, NOT an entry in `lib/icons.ts` — PORT-024 scoped `IconName` to icons a content type names, and a spinner has no content behind it. **The hidden honeypot is load-bearing markup**, not decoration: PORT-040 made the field required in the schema, so the form does not parse without it. It sits in a `.honeypot-field` wrapper (a real CSS rule in `globals.css`, because `left: -9999px` is an arbitrary value ui-rules §1 forbids inline) with `aria-hidden` on the **wrapper** and `tabIndex={-1}` on the input — `aria-hidden` on a focusable control is itself an ARIA violation, and `sr-only` is wrong here because it deliberately keeps content available to screen readers. Its `id` is `contact-website`, not `contact-honeypot`, so a password manager does not autofill the trap and silently classify a real person as a bot. Idle/pending/error render one `<form className="space-y-6">`; **success replaces the form entirely** with a `role="status" aria-live="polite"` panel (`border-fern bg-surface-2 rounded-lg border p-6`) carrying a "Send another" `outline sm` Button that resets. Field errors render through `Field`'s own `error` prop (already `role="alert"`); the form-level banner is only for the failure no single field owns and carries the `mailto:` fallback §6 requires; a third `sr-only aria-live` paragraph announces the validation summary, since a visitor who submits from the button otherwise hears nothing about why the page did not move. Pending disables the submit, swaps its label to "Sending…", shows the spinner **and** announces in a sibling `aria-live="polite"` paragraph. **`noValidate` on the `<form>` is load-bearing**: without it the browser's own bubble intercepts an empty submit and preempts the authoritative server parse.  **PORT-043 added a SECOND piece of load-bearing hidden markup**, `<input type="hidden" name="startedAt">`, carrying the mount time the server's minimum-time guard measures against — delete either it or the honeypot and every submission fails. It is a plain hidden input, not the honeypot's off-canvas treatment: hidden inputs are already unfocusable and out of the accessibility tree, whereas the honeypot only lives off-canvas because it has to look real to a bot. The value comes from `useState(() => Date.now())` — **not** `useRef`, which ESLint rejected on two counts (calling the impure `Date.now()` during render, and reading `.current` during render) and which also called it on every render only to discard the result. `suppressHydrationWarning` is required, because the server stamps its own clock and the client stamps a different one on hydration; the client value is the one that survives and gets posted, which is what the guard needs. The `formKey` remount gained a second job here: it issues a **fresh** timestamp, without which "Send another" would reuse the first message's mount time. The rate limit renders through the **existing** form-level banner — no new markup, because it is precisely a failure no single field owns. |
| `ContactMethods` | **built** | `src/components/sections/contact-methods.tsx` | no | `/contact`. Props: `className?` — no content props; reads `site` directly (the Header/Footer/Hero/AboutIntro carve-out, content-model.md §4). Wrapper `space-y-8`: an `<h2>` + blurb, then a `<ul class="space-y-4">` of labelled rows (`Mail` → `site.email` as a `mailto:`, `MapPin` → `site.location`), then the socials. Each row is `flex items-start gap-3` with a `text-fern mt-0.5 shrink-0` `<Icon>` and an eyebrow-styled `<p>` label above the value. **Filters `email` out of `site.socials`** — the address already has its own labelled row, so without the filter the same `mailto:` renders twice on one page (asserted in the browser checks). Carries its own `PLATFORM_TEXT` Record for the same WCAG 2.5.3 reason `Footer` does: `site.socials[].label` is the accessible name, the map holds the shorter visible word, and a new platform in `types.ts` fails the build here rather than rendering `undefined`. Socials open in a new tab (`target="_blank" rel="noreferrer noopener"`); the `mailto:` does not. |
| `AboutIntro` | **built** | `src/components/sections/about-intro.tsx` | no | `/about`. Props: `className?` — no content props at all; reads `site` directly for name, role, location and photo, the same carve-out `Header`/`Footer`/`Hero` use (content-model.md §4). Owns the page's single `<h1>`. Wrapper `grid items-start gap-10 lg:grid-cols-3 lg:items-center lg:gap-16`; the text column takes `lg:col-span-2` and the portrait the third, so at `lg` it is a 2:1 split and below `lg` a single stack. Eyebrow `text-eyebrow text-faint mb-4 font-mono uppercase` reading “About”; `h1` `text-h-lg text-ink`; meta as `<ul class="text-muted mt-6 flex flex-wrap gap-x-8 gap-y-3 text-sm">` of `<li class="inline-flex items-center gap-2">` each carrying an `<Icon>` (`Briefcase` → `site.role`, `MapPin` → `site.location`) — **the first real use of either name**, both added by PORT-024 for exactly these two `SiteConfig` fields. Portrait is `next/image` with the asset's **real `width`/`height`** (like `ProjectHeader`'s cover, unlike the fixed-ratio boxes): `border-border mx-auto h-auto w-full max-w-2xs rounded-lg border lg:mx-0 lg:max-w-none`, `priority` because it is above the fold. **Deliberately does not repeat `site.tagline`** — `Hero` already leads with it on `/`. **`lg:items-center` is load-bearing, not decoration:** the portrait is roughly four times the height of the two text lines beside it, and top-aligned it left ~250px of visible dead space under the name at 1440 and 1024. |
| `Cta` | **built** | `src/components/sections/cta.tsx` | no | `/`, `/skills` (planned), `/about` (planned) — `.cta-panel`. Props: `heading: string`, `body?: string`, `primary: { label, href }`, `secondary?: { label, href }`, `className?`. Content-free by design — every caller supplies its own copy/links; only `/` (PORT-030) actually consumes it so far. Panel is `border-border bg-surface-2 rounded-xl border px-8 py-12 text-center`. **Visual treatment is an unmeasured judgment call** — same prototype-not-found caveat as the dark shadow tokens (ui-rules.md §3). |
| `CaseStudy` | **built** | `src/components/sections/case-study.tsx` | no | `/projects/[slug]`. Props: `problem: string`, `approach: string`, `outcome: string`, `highlights?: string[]`, `className?`. Wrapper `space-y-12`; each block is a `<section>` whose label is a real `<h2>` wearing the eyebrow style — `text-eyebrow text-faint font-mono uppercase` — followed by `<Prose className="mt-4">`. Takes four plain values, **not** a `Project`: a case study is a shape (heading, then prose), not a project, so anything with that shape can reuse it. `highlights` renders a fourth block only when present and non-empty. |
| `ProjectHeader` | **built** | `src/components/sections/project-header.tsx` | no | `/projects/[slug]`. Props: `project: Project`, `className?`. Owns **every** optional-field branch on the page so the route file stays flat. Eyebrow `text-eyebrow text-faint mb-4 font-mono uppercase` reading `{year} · {STATUS_TEXT[status]}`; `h1` `text-h-lg text-ink`; summary `text-lead text-muted mt-6 max-w-measure`; role/duration as a `<dl>` `mt-8 flex flex-wrap gap-x-10 gap-y-4` (skipped entirely when both absent); stack as `<ul aria-label="Built with" class="mt-8 flex flex-wrap gap-2">` of `<li><Badge></li>`; live/repo links `text-fern hover:text-fern-hover inline-flex items-center gap-1.5 rounded-md text-sm font-medium transition-colors` + focus ring + `ArrowUpRight` at 14, `target="_blank" rel="noreferrer noopener"`. Cover is `next/image` with the asset's **real `width`/`height`** (not `aspect-thumbnail`) — `border-border mt-12 h-auto w-full rounded-lg border`, `priority` because it is the LCP element. `cover ?? thumbnail` per types.ts. **Status words are a full `Record<ProjectStatus, string>`** so a fourth status fails the build here. **Links keyed on `label`, never `href`** — opalusph-website carries the same placeholder URL twice. |
| `ProjectGallery` | **built** | `src/components/sections/project-gallery.tsx` | no | `/projects/[slug]`. Props: `images: ImageAsset[]`, `className?`. Returns `null` on an empty list. `<h2>` in the eyebrow style, then `mt-6 grid grid-cols-1 gap-6 md:grid-cols-2`; each image is `next/image fill` inside `aspect-thumbnail border-border relative w-full overflow-hidden rounded-lg border`. Unlike `ProjectHeader`'s cover it **does** use the fixed ratio box — it is a grid, and one odd-sized cell breaks the row. The caller must still skip the wrapping `<Section>`, or an absent gallery leaves a band of blank padding. |
| `ProjectNav` | **built** | `src/components/sections/project-nav.tsx` | no | `/projects/[slug]`. Props: `previous?: Project`, `next?: Project`, `className?`. Returns `null` when both are absent. `<nav aria-labelledby="more-projects">` + `<h2 id="more-projects">` in the eyebrow style, then `mt-6 grid grid-cols-1 gap-4 md:grid-cols-2`. Each card is a `next/link` (`group block rounded-lg` + focus ring, `aria-label={`${direction} project: ${title}`}`) wrapping a `<Card padded>` with `h-full transition-colors group-hover:border-fern group-focus-visible:border-fern` — the same wrap-not-prop composition as `ProjectCard`. `next` gets `md:text-right` + `md:justify-end`; arrows are `ArrowLeft`/`ArrowRight` at 14. **A missing side renders `<div aria-hidden />` as a spacer**, so "previous" always sits left and "next" always sits right — without it the lone card on the newest/oldest project lands in the wrong grid cell and reads as the wrong direction. |

---

## 5. States

Every one of these must exist somewhere visible before the site ships. Tracked here because they are the things most often skipped and then discovered in production.

| State | Where it appears | Status |
|---|---|---|
| Empty — filter matches no projects | `/projects` | **built** — `ProjectGrid`'s `emptyState` prop, supplied by the page with a "Reset filter" `Button` back to `/projects` |
| Empty — a project has no `liveUrl`/`repoUrl` | `/projects/[slug]` | **built** — `ProjectHeader` builds the link list from whichever URLs are present and renders no `<ul>` at all when it is empty. Same pattern for `role`/`duration` (no `<dl>`), `highlights` (no block), `gallery` (no block, and the page skips the `<Section>` too). Verified on `construction-company-website`, which has none of them. |
| Empty — no jobs / no education | `/resume` | **built** (PORT-034) — `Timeline`'s required `emptyState` prop; the page supplies a sentence for each list. **Unexercised by real content** (both lists are non-empty), so it is proven by the prop being required, not by a screenshot. |
| Print — `/resume` on paper | `@media print` in `globals.css` | **built** (PORT-034) — verified by emulating print media **from a dark-mode page** and rendering a real A4 PDF: header, footer, skip link and the Download button all `display: none`, body forced to `#fff` / `#000`, section padding zeroed. Breaks across 2 pages between two skill tiers; `break-inside-avoid` kept every timeline entry and tier whole. |
| Pending — contact form submitting | `/contact` | **built** (PORT-036) — submit disabled + label "Sending…" + a sibling `aria-live="polite"` paragraph announcing it. Observed at 4 widths × both themes. |
| Success — message sent | `/contact` | **built** (PORT-036) — the form is **replaced** by a `role="status"` panel, not appended to, so there is no stale form under a success message. "Send another" returns to a clean idle form (asserted, not assumed). |
| Error — field validation | `/contact` | **built** (PORT-036) — rendered through `Field`'s own `error` prop: `aria-invalid="true"`, `aria-describedby` pointing at the error id, `role="alert"`, `border-coral` on the control and `text-coral-text` on the message. Verified on all three fields at every width and theme. |
| Error — email provider failed, with `mailto:` fallback | `/contact` | **built** (PORT-036) — a form-level `role="alert"` banner (`border-coral bg-surface-2`) with the `mailto:` next step code-standards.md §6 requires. Distinct from field validation: the banner renders only when `fieldErrors` is absent, so the two error states never stack. Reachable in the mock by putting `fail` in the email. |
| 404 — unknown route | `src/app/not-found.tsx` | **built** (PORT-006) |
| 404 — unknown project slug | `src/app/projects/[slug]/not-found.tsx` | **built** (PORT-032) — mirrors the root 404's structure exactly; the only difference is the exit, which goes to `/projects` rather than `/`. Next walks *up* from the segment that called `notFound()` and uses the first `not-found.tsx` it finds, so this file must live inside `[slug]/` or the root one silently wins. Verified: `/projects/does-not-exist` returns a real HTTP 404 in both themes. |
| 500 — render error | `src/app/error.tsx` | **built** (PORT-006) |

---

## Change log

| Date | Component(s) | Change |
|---|---|---|
| 2026-09-05 | *(no component)* — OG cards | **PORT-050** — two `opengraph-image.tsx` files, which are NOT components and are listed here only so nobody looks for them in this registry. They are rendered by Satori inside `next/og`, not by React: **flexbox only, no cascade, no stylesheet, and Tailwind classes do nothing at all — silently.** Every rule is an inline style object, every multi-child element needs an explicit `display: flex` or Satori throws, and colours must be **hex**: the whole token system is `oklch()`, which Satori cannot parse and skips without a warning, rendering the card black on transparent with a green build. The palette in `lib/seo.ts` duplicates the source hexes ui-rules §3 records — a knowing duplication, because the renderer cannot read the stylesheet. The font is Geist, bundled with `next/og`, so no font file and no build-time fetch. **The cards deliberately carry no imagery** — typography and one fern rule — which is what let `/about` reverse PORT-033's no-`og:image` opt-out: a card that never touches `site.photo` cannot leak the "PHOTO PENDING" placeholder. |
| 2026-09-05 | `ContactForm` (hidden timestamp) | **PORT-043** — the spam guards. No new component; one hidden input and one hook. The value is `useState(() => Date.now())`, and the shape was **decided by the linter, not by preference**: the first build used `useRef(Date.now())` with `startedAt.current` read in the JSX, and `react-hooks/purity` plus `react-hooks/refs` both fired, both correctly. Passing `Date.now()` as useRef's argument evaluates it on *every* render and throws the result away — it looked stable while breaking the purity rule for nothing. The lazy `useState` initialiser runs once, on mount, outside the purity constraint, and is legitimately readable during render. **`suppressHydrationWarning` is genuinely required here**, the same class of legitimately-client-only value as the `next-themes` rule: the server prerenders its own millisecond and the client replaces it on hydration. **The bug worth carrying is about the failure SHAPE, not the markup.** The action strips hidden-field errors from `fieldErrors` so no visitor sees an error for a field they cannot see — but when a hidden field was the *only* failure, that left `{}`, which is truthy, so the form rendered "Please check the fields below" above three clean fields, showed no banner (the banner only renders when `fieldErrors === undefined`), and dead-ended the visitor. **That is the same dead-form failure PORT-041 removed for the honeypot, grown back through a different door** — and the assertion passed, because it only checked that no hidden field name leaked, which was true. Caught by reading the rendered page. A failure no visible field owns must now omit `fieldErrors` entirely. A second, cosmetic one went the same way: the rate-limit message ended with "email me directly" while the banner appends its own `mailto:` sentence, so a blocked visitor read the fallback twice — invisible in source, obvious in the browser. |
| 2026-09-03 | `ContactForm` (rewired), `Spinner` | **PORT-041** — the form now submits to a real Server Action. `useActionState(submitContact, null)` replaced `useState`; `mockValidate` and the `setTimeout` are deleted. PORT-036 promised this would touch no markup and that held for the swap itself, but **three structural additions were forced, each by something that only showed up when it ran.** (1) **"Send another" as a `<Link href="/contact">` was DEAD.** `useActionState` exposes no reset, so the plan was to navigate back to the page — but the visitor is already on `/contact`, the client router treats it as a same-route navigation, the component stays mounted, and the success result lives forever. A hard reload cleared it, which is what proved the state is client-only and a *remount* is the fix: `ContactForm` is now a wrapper holding a `formKey`, the form moved into `ContactFormFields`, and "Send another" bumps the key. (2) **`SubmitButton` split out**, because `useFormStatus` reads the PARENT form and returns `pending: false` forever when called inside the component that renders that form — the gotcha CLAUDE.md already recorded, met in the wild. (3) **A `Spinner`**, because the AC asks for one and none existed; inline `<svg className="animate-spin" aria-hidden>` rather than a `lib/icons.ts` entry, since PORT-024 scoped `IconName` to icons a content type actually names and a spinner has no content behind it. Note the global `prefers-reduced-motion` block sets `animation-duration: 0.01ms`, which **freezes** the spinner rather than hiding it — acceptable because the text carries the meaning, but it is a static circle for those users, not an absent one. **The honeypot got a real CSS rule, `.honeypot-field`**, not a utility class: `left: -9999px` is an arbitrary value ui-rules §1 forbids inline, `display:none` would defeat the trap (a bot rendering CSS skips visibly-hidden fields), and **`sr-only` is exactly wrong** — it is built to keep content available to screen readers. `aria-hidden` goes on the **wrapper**, never the focusable input, which would be an ARIA violation on its own. The input's id is `contact-website`, not `contact-honeypot`, so a password manager does not fill the trap and silently classify a real person as a bot — a false positive here is invisible and unrecoverable, since a caught bot is shown success. **A real bug was caught by a browser check and would not have been caught by reading:** the honeypot was originally checked *after* `safeParse`, on the reasoning that the schema rejects a filled one anyway. It does — and that was the bug. The rejection came back as `fieldErrors: { honeypot: [...] }`, no visible `Field` renders that key, and the form simply refused to submit with **no reason shown at all**. The check moved ahead of the parse, reading raw `FormData`, so a caught bot gets `{ ok: true }` as designed; and if `honeypot` ever fails the schema anyway (the hidden input going missing from the markup), that key is stripped from `fieldErrors` and logged server-side rather than surfaced as an error no field can display. |
| 2026-09-02 | `SkillTier`, `PracticeCard` | Built (PORT-037) — **Sprint 3 complete**, both were the last two `designed` rows this table carried for a page that existed. `SkillTier` is the full treatment `/about` deliberately did not grow into (see this table's 2026-08-25 `AboutIntro` row); the compact inline block on `/about` stays as it is, and the two have not converged. **This table's own `.tier-dot` design — saturation — was dropped on measurement, and that is the entry worth reading:** wash tokens (`fern-wash`, `surface-2`) rendered at 1.06:1/1.02:1 light and 1.23:1/1.22:1 dark against the page ground, well under WCAG 1.4.11's 3:1 for a meaningful non-text mark, and looked like three empty rings on screen in both themes. `fern` at 70%/45% opacity was the obvious repair and still failed at 2.79:1 and 1.86:1 in light — the real cause being headroom, since `--fern` is only 4.78:1 against the light ground to begin with. Depth is now **filled count plus dot size** at full-strength fern (4.78:1 light, 7.81:1 dark), which encodes it twice without touching colour strength. The general rule, added to ui-rules §5: **a wash token is a background tint and must never be used as a foreground mark.** `PracticeCard` consumes the first four of the eight `Practice` icon names PORT-024 reserved for exactly this content, two tickets ahead of it existing. **A heading-order break was also caught in this pass** — the page's three tier `h3`s hung off no `h2` (`h1 → h3`, WCAG 1.3.1) because the intro was folded into the first `Section` to avoid PORT-036's stacked-padding void; fixed with an inline `<h2>` rather than `Section`'s `heading` prop, which renders above `children` and would have sat above the page's own `h1`. **254/254 Playwright assertions** across 1440/1024/768/375 × light/dark on a real `next start`, screenshots read at every combination. |
| 2026-09-02 | `ContactForm`, `ContactMethods` | Built (PORT-036), the UI half of the deliberate UI-before-wiring split — **no Server Action, no Zod, no `lib/validation/`**, all of which are Sprint 4. Both had sat `designed` since the prototype was catalogued. **`ContactForm` is built as a Client Component now rather than converted later**: its `FormState` union is deliberately shaped like the `ActionResult` union code-standards.md §6 specifies, so PORT-041 swaps `useState` for `useActionState`, deletes `mockValidate` and the `setTimeout`, and touches no JSX. The throwaway validation is **not** a client-side Zod schema on purpose — the real one lives in `lib/validation/`, is shared with the server, and the server parse is authoritative; writing half of it in the wrong layer to move later is worse than checks that are obviously temporary. **Three a11y decisions worth keeping.** `noValidate` on the form is load-bearing, not stylistic: without it the browser's native bubble intercepts an empty submit, which both hides the designed validation state *and*, in PORT-041, would preempt the authoritative server parse. Pending announces in a sibling `aria-live="polite"` paragraph as well as changing the button label, because a label change on a control you are not focused on is a visual-only cue. Success **replaces** the form rather than rendering above it, so no stale filled-in form sits under a "Message sent" panel. `ContactMethods` filters `email` out of `site.socials` — the address has its own labelled row, and without the filter the identical `mailto:` renders twice on one page. **The layout fault was caught by reading a screenshot after the numbers came back clean — the same way PORT-034's `flex-wrap` was.** The first build put the page intro in its own `<Section spacing="tight">` above the form's grid; 454/454 assertions passed, zero overflow at every width, and it still looked broken: two tight sections stack their padding, so there was a measured **128px void** between the lead paragraph and the first field at 1440 (113 at 1024, 72 at 375 — it scales with the breakpoint because the token is fluid), and the intro's right half was empty while the methods column started level with the first input. Fixed by collapsing to **one** `Section` with the intro inside the form's own grid column: the gap is now a uniform 48px at every width and both columns start at the top. `/about` stacks two tight sections for a similar shape and reads fine — the difference is that its second section is body prose continuing the first, whereas here the second section was the thing the page exists for. `npm run verify` green; `/contact` still `○` static despite the client form. **454/454 Playwright assertions green** across 1440/1024/768/375 × light/dark on a real `next start` production server, driving all four states through the real form at every combination: one `<h1>`, no skipped heading level, no horizontal overflow, three labelled required fields with correct `name`/`type`/element, exactly one `mailto:` in `main` (the duplicate-email regression test), idle → field-validation → pending → provider-failure → success → back to idle, `aria-invalid`/`aria-describedby`/`role="alert"` on all three fields, the `mailto:` fallback inside the failure banner, no stale field errors in the provider-failure state, zero console errors, plus a keyboard-only pass (tab order name → email → message → submit with a visible focus indicator at every stop, and Enter inside a text field submitting the form) and metadata read from the served HTML (`og:type=website`, absolute `og:url`, **no `og:image`** — PORT-050). One assertion was wrong on the first run and the component was fine: `[role="alert"]` was scoped to the document, which catches Next's own visually-hidden `#__next-route-announcer__` present on every route; scoping it to `main` was the fix. Full-page screenshots at 1440/768/375 × both themes read directly. |
| 2026-09-02 | `Timeline` | Built (PORT-034), the first `sections/` component consumed by two different content types. Registry had it as `designed` with prototype classes `.tl` / `.tl-item--now`, **neither of which existed anywhere in `src/`** — they were prototype names, so the entry above is the first real record of what the component is. **A layout fault was caught in the browser and fixed before close, not by review:** `flex flex-wrap` on the title/date row passed every measurement (zero overflow at all five widths) yet rendered one list in two different layouts below `md`, because each item wrapped on its own according to how long its title happened to be. Now `md:flex`, so the breakpoint decides once for every item. Verified at 1440/1000/768/460/375 in both themes on a real `next start`, plus 760/759 to confirm the flip lands exactly on the design's breakpoint. Also added: `src/lib/dates.ts` (**no `Date` object anywhere in it** — `new Date("2026-02")` is midnight UTC and renders as January in any timezone behind UTC, a bug that would pass locally and ship broken) and a global `@media print` block in `globals.css`, keyed on `body > header` / `body > footer` / `a[href="#main"]` because the shell is rendered by the root layout and no page can reach it. |
| 2026-08-25 | `AboutIntro` | Built (PORT-033), the only new component `/about` needed. **Two components were deliberately NOT built for that page.** `SkillTier` stays `designed`: About renders the *compact* skills treatment (tier label + what the tier means + `Badge` list) and PORT-037 owns the dotted, saturation-weighted `/skills` version — building it early would be building ahead, and building something adjacent would be the near-duplicate this table exists to prevent. Nothing was built for the bio either: it is JSX inside `Prose` in the page file, because it carries an inline `<Link>` and markup inside a typed content string means a markdown renderer or HTML-in-content, both ruled out by build-plan §9. **`AboutIntro` takes no content props**, unlike every `Project*` component on this table — `ProjectHeader` takes a `Project` because there are four of them, whereas there is exactly one person on this site, so threading their own name through a prop would invent a configurability nothing wants; `Hero` set that precedent. **One new content field shipped with it:** `SiteConfig.photo`, **required** rather than optional, so there is no dead no-photo branch to design and `site.ts` fails the build if it is missing. The portrait it renders is a generated placeholder (`PHOTO PENDING`, 1000×1250, 4:5) — Vernel's call, tracked by the new **PORT-058**, same pattern PORT-057 set for the project images. **Two layout faults were found by looking at the screenshots and fixed before close**, each tested live in the browser first rather than reasoned about: `lg:items-center` on the intro grid (top-aligned, a 400px portrait against two lines of text left ~250px of dead space at 1440 and 1024), and the page's skill grid moved `md:grid-cols-3` → `lg:grid-cols-3` (at 768 three ~220px columns broke mono badges mid-phrase — “Role-based access / control” inside one chip; confirmed one column at 768 and three at 1024 by reading the computed `grid-template-columns`, not by eye). `npm run verify` green; `○ /about` static. **190/190 Playwright assertions green** across 1440/1024/768/375 × light/dark on a real `next start` server: one `<h1>` inside the initial viewport at every width, no skipped heading level, zero horizontal overflow, portrait loaded (`naturalWidth > 0`) with real alt at a measured 4:5 and preloaded rather than lazy, 19 skill badges, `/resume` + `/contact` + `/projects` all linked, in-prose link focusable with a real outline, **zero console errors**, and metadata read from the served HTML — `og:type=profile`, absolute `og:url`, and **no `og:image`** while the portrait is a placeholder. Full-page screenshots at 1440/1024/768/375 × both themes eyeballed directly. |
| 2026-08-25 | `ProjectHeader`, `CaseStudy`, `ProjectGallery`, `ProjectNav` | Built (PORT-032), plus the segment 404 at `src/app/projects/[slug]/not-found.tsx`. `CaseStudy` is the row this table had carried as `designed` since 2026-08-18 — built as described ("eyebrow-led prose blocks"), with each label a real `<h2>` wearing the eyebrow type style rather than a decorative `<p>`, so the page stays navigable by heading while looking the way the prototype describes. The other three are genuinely new; nothing existing was close. **`ProjectHeader` takes the whole `Project`, `CaseStudy` takes four plain strings** — deliberate asymmetry: the header needs most of the object and the content type is already its contract, whereas a case study is a *shape* (heading, then prose) that anything could reuse without owning a Project. **`ProjectNav` reuses `ProjectCard`'s wrap-not-prop composition** — a `next/link` around a plain `Card`, `group-hover:` reaching the inner border — rather than teaching `Card` about links, per the 2026-08-24 decision that closed that question. Arrows (`ArrowLeft`/`ArrowRight`/`ArrowUpRight`) are imported straight from `lucide-react`, not added to `IconName`: PORT-024 settled that `IconName`'s members are each tied to a content type, and a directional arrow is chrome, not content — the same precedent `Footer`/`MobileMenu`/`ThemeToggle` already set. **The cover image deliberately does NOT use `aspect-thumbnail`**, unlike `ProjectCard`'s thumbnail and this ticket's own gallery: a card lives in a grid where an odd height breaks the row, but a page header is a single full-width element where a taller image shifts nothing beside it, so the header honours the asset's real `width`/`height` — which is what those `ImageAsset` fields exist for. Measured consequence, worth knowing: a project falling back to its 16:10 thumbnail renders a **671px-tall** header at 1440 against **448px** for a real 12:5 cover — 50% taller, not shorter. Flagged to Vernel as a look-right call; the prototype that would settle it is still not in the repo. `npm run verify` green; `next build` shows `● /projects/<slug>` for all four projects. **497/497 Playwright assertions green** across 1440/1024/768/375 × light/dark on a real `next start` production server, every project visited at every combination: HTTP 200, theme resolved per `colorScheme` before `goto`, zero horizontal overflow, exactly one `<h1>` with no skipped heading level, all three case-study blocks present, no nested `<a>`, header image actually loaded (`naturalWidth > 0`) with real alt text, stack badges rendered, and **zero console errors — the `/projects/[slug]` prefetch 404s that Home and `/projects` both carried in their sign-offs are gone, because this ticket is what created the route.** Optional-field behaviour verified per project rather than in aggregate: `construction-company-website` (no cover, gallery, highlights, role, duration or links) renders no `<dl>`, no Highlights, no Gallery, no external links and falls back to its thumbnail; `cict-project-gate` uses its real cover and 4 highlights with no gallery; `grades-repository-system` renders both gallery images loaded; `opalusph-website` renders `Role`/`Duration` and both external links with `rel` carrying `noopener`+`noreferrer`. Nav correctness asserted by `aria-label` text and geometry, not just link count: the newest project gets one "Next" card and it is confirmed to sit in the **right** half of the grid (x=728 vs. midpoint 720), the oldest gets one "Previous", the middle ones get both in the right order. `generateMetadata` checked per project — title, description, `og:title`/`og:description`/`og:url`/`og:type`/`og:image` (absolute, built from `site.url`, with real 2400×1000 dimensions and alt) — and confirmed to differ between two projects rather than being one shared blob. Keyboard pass: a nav card is Tab-reachable with a real visible focus ring and Enter follows it. Full-page screenshots at 1440/375 × both themes eyeballed directly, including the 404. |
| 2026-08-24 | `Chip`, `ProjectFilter`, `ProjectGrid` | Built (PORT-031). `Chip` (`ui/`) is a new primitive this table had already named for the job — a pill toggle button, `aria-pressed` carrying real selected/not-selected state rather than just a color swap. `ProjectFilter` (`sections/`, the ticket's one Client Component) renders "All" plus one `Chip` per tag from `getAllTags()` and writes the choice to the URL via `router.push`; it takes `activeTag` as a **prop** from the server page instead of calling `useSearchParams()` itself, which would have required wrapping it in `<Suspense>` to avoid de-opting the page from static rendering — passing the already-awaited value down is a strictly smaller client footprint for the same result. `ProjectGrid` (`sections/`, Server) is the shared card grid this table already named for both `/` and `/projects` — `grid-cols-1 md:grid-cols-2 lg:grid-cols-3` per PORT-031's AC — plus an `emptyState?: ReactNode` slot so `/projects` can supply "no projects at all" vs. "no match for this tag, here's a reset" without the component knowing which is which. **`/projects` reads `searchParams` server-side to filter**, which keeps `ProjectCard` fully server-rendered (the ticket's own "watch for": don't make the whole page a Client Component to hold filter state) but also opts the route out of static generation — `next build` now shows `ƒ /projects` where every other route is `○`, confirmed in real build output, not assumed. **`ProjectGrid` is not yet wired into `/`** — Home's featured grid (PORT-030) still inlines its own `div` with a different 1→2-column breakpoint (`sm:` vs. this component's `md:`); flagged rather than silently reconciled, since PORT-030 is already closed and touching it wasn't asked for. `npm run verify` green. **152/152 Playwright assertions green** across 1440/1024/768/375 × light/dark on a real `next start` build: exactly 4 cards with no filter, "All" pressed by default, a chip click updates both the URL and its own pressed state, the filter survives a hard refresh (proving state comes from the URL, not client memory), the browser back button reverts to the previous filter, an unknown tag renders zero cards plus a working "Reset filter" `Button`, no horizontal overflow at any breakpoint, a chip is Tab-reachable with a visible focus ring. The only console errors seen — excluded from that count — are `<Link>` prefetching `/projects/[slug]`, not yet built (PORT-032); the identical, already-documented condition Home's own verification carries. |
| 2026-08-24 | `Hero`, `Cta` | Built (PORT-030) — first Sprint 3 ticket, first page rendering real content. **Scoped to build-plan's own AC, not the fuller `/`-page set this table already named** — `StatusPill`/`StatRow`/`StackStrip` stay `designed`: none has source copy written anywhere, and the approved prototype they're drawn from is confirmed absent from the repo (this table's own rows for the dark shadow tokens already flagged that gap). Raised with Vernel directly; he chose to build only what the AC asked for. `Hero` reads `site` directly for role/name/tagline — no props — the same carve-out `Header`/`Footer` already use (content-model.md §4); it renders no buttons, since PORT-030's own description reads as three parts ("Hero, featured projects, CTA") and both CTAs live in the closing `Cta` band instead. `Cta` is content-free (`heading`/`body`/`primary`/`secondary`/`className` props) rather than hardcoding Home's message, since this table already named it as shared across three pages — the day `/skills` or `/about` need one, they pass their own copy rather than editing the component. Its panel styling (centered, `bg-surface-2`, `rounded-xl` border) is an unmeasured judgment call, not derived from anything — same open caveat as the dark shadow values. **Zero client JS confirmed, not assumed**: grepped real `.next/static/chunks/*.js` after a `next build` for Home's own copy strings — zero hits, so `Hero`, `Cta` and the featured-projects `ProjectCard` grid all render server-side, same pattern PORT-024 already proved for `Icon`. **69/69 Playwright assertions green** across 1440/1024/768/375 × light/dark against a real `next start` production server (not dev mode): theme resolved correctly per `page.emulateMedia` before `goto`, zero horizontal overflow at any width, hero `h1` and tagline both confirmed within the initial viewport at 375px, exactly three featured cards with no nested `<a>`, `Cta` heading and both buttons present and correctly linked (`/contact`, `/projects`). Three console 404s per page load are expected, not a regression — Next's `<Link>` prefetch hits `/projects/[slug]`, which doesn't exist until PORT-032 (already an open, documented gap). Full-page screenshots taken at 1440/375 × both themes and eyeballed directly: clean grid, legible CTA panel, no broken layout in either theme. |
| 2026-08-24 | `Icon` | Built (PORT-024) — **Sprint 2 complete.** `src/lib/icons.ts` holds `ICONS: Record<IconName, LucideIcon>`, 12 names each tied to a content type already built (8 for `Practice`, 2 for `Job`/`Education`, 2 for `SiteConfig.email`/`.location`) — nothing invented for the `practices` content PORT-037 hasn't written yet. `IconName` lives in `content/types.ts`, not `lib/`, keeping `content → lib` the one correct dependency direction; `Practice.icon` narrowed from `string` to `IconName`. `Icon` itself derives its prop type from the map's own component type rather than hand-typing it, so it can't drift from lucide-react's real surface. **Bundle impact verified by grepping the real production output**, not assumed from "per-icon imports are used": none of the 12 names appear in any `.next/static/chunks/*.js` file, because `Icon` and every consumer of it are Server Components — the whole registry renders to static SVG server-side and ships zero client bytes. The pre-existing direct `lucide-react` imports in `footer.tsx`/`mobile-menu.tsx`/`theme-toggle.tsx` were left alone — confirmed via the library's own source that they already get `aria-hidden` for free, so sweeping them would have been unrequested scope on three closed tickets. Verified via the real `/gallery` route (PORT-025, same session) rather than a throwaway probe. |
| 2026-08-24 | `Field`, `Input`, `Textarea` | Built (PORT-023), the accessibility-critical ticket, and open question 7 settled in the same session — see the `--coral-text` entry in `ui-rules.md` §3. **`Field` derives `id` from a new required `name` prop (`` `field-${name}` ``) rather than `useId()`** — the registry's planned API didn't list `name`, but `useId` is a hook, and hooks don't run in Server Components, so using it would have forced `Field` (and by extension anything composing it) into a Client Component for no interactive reason. Every field needs a unique `name` anyway for the eventual Server Action's `FormData`, so this isn't a new constraint on callers, and it keeps `Field`/`Input`/`Textarea` all Server Components — code-standards.md's "push `use client` to the leaf" applied literally; the real leaf is the future `<ContactForm>` (PORT-041). **The control is wired via a render prop**, exactly as the registry named it: `Field` builds `id`/`name`/`aria-describedby`/`aria-invalid`/`required` and hands them to a function child, so it never imports `Input` or `Textarea` and works with either. `aria-describedby` joins whichever of the hint/error paragraph ids actually exist (hint before error), and the error paragraph carries `role="alert"` for a live announcement in addition to the on-focus description read from `aria-describedby`. **The required mark and error text use the new `--coral-text` token, not `--coral`** — `--coral` stays the border/status-dot color (`aria-invalid:border-coral`, still only needing the 3:1 non-text floor); `--coral-text` is what actually clears 4.5:1 for the two places coral is genuine text on this site. `npm run verify` green, 7/7 routes static. **202/202 Playwright assertions green** across 1440/1024/768/375 × light/dark via a throwaway `/dev-probe-023` route (five fields covering every optional-field combination: required-only, required+hint, error-only, required+error on a `Textarea`, hint-only on a `Textarea`) — contrast on both new-token uses (5.01:1 light / 6.99:1 dark, canvas-pixel-sampled, both a full point clear of the 4.5:1 AA floor), `aria-invalid="true"` + matching `border-coral` on both error fields, `aria-describedby` id sets and join order correct on every field, required attribute + visible `aria-hidden` asterisk present exactly where `required` was passed and absent elsewhere, label `click()` focusing its control, `resize: vertical` (never `both`/`none`) on both textareas, and a real keyboard-only pass — click into the first field, Tab through the rest, confirm a visible focus ring at every stop and that each error field's accessible description (resolved from `aria-describedby`, not just DOM presence) actually contains its error text. **The first tab-order test run was wrong, not the component**: it started from a scripted `.blur()`, which doesn't reliably reset Chromium's tab-sequence anchor, and the probe renders inside the real site shell (`SkipLink`, header nav, `ThemeToggle`, burger) — so a truly document-top start would have walked through header chrome first, which isn't what the AC is asking about. Fixed by clicking directly into the first field as a real action and asserting order only among the five fields themselves. Probe route deleted after. |
| 2026-08-24 | `Badge` | Built (PORT-022). A styled `<span>` — the exact chip classes `ProjectCard` already inlined, now shared. **Ships neutral tone only, not the prototype's `tone: neutral\|fern\|coral`.** That mismatch was a real fork, not a silent call: the row above had recorded three tones since the prototype was catalogued 2026-08-18, but `ui-rules.md` §6's built-component contract only ever had the one class string, PORT-022's AC just says "renders a tag/stack chip", and the only consumer (`ProjectCard`'s tags) only needs neutral. `coral` also reopens open question 7 (unresolved contrast) inside a ticket that has no reason to touch it. Put to Vernel directly rather than defaulted either way; he chose neutral-only. `fern`/`coral` land as a `tone` prop the day a real consumer needs one — at which point coral's color has to actually settle the open question, not dodge it a second time. Verified via a throwaway `/dev-probe-022` route (real `ProjectCard` grid + a raw 8-badge row, deleted after): 50/50 Playwright assertions across 1440/1024/768/375 × light/dark — no overflow at any width, wrap asserted only at 375 (6 lines; 1440 correctly stays one line, no requirement to wrap there), no nested `<a>`, contrast on both `ground` and `surface` well above AA in both themes (badge-on-ground exactly matches `Button` ghost's already-recorded number, since both are bare `text-muted`). Contrast had to be read via canvas pixel-sampling, not `getComputedStyle` string parsing — this Chromium serialises the oklch tokens as `lab(...)`, not `rgb(...)`. |
| 2026-08-24 | `Card`, `ProjectCard` | Built (PORT-021). **`Card` stays a plain `<div>`, never a link itself** — the AC's "whole card is one link" is satisfied by `ProjectCard` wrapping a `next/link` *around* `Card`, not by making `Card` polymorphic the way `Button` is. That keeps `Card` genuinely generic (no `href` prop, no knowledge of being clickable) and proven by the lint rule rather than by convention — `@/content/types` inside `src/components/ui/` fails `no-restricted-imports`, checked with the `--stdin` probe technique from PORT-015/020. **The whole-card accessible name is forced with `aria-label={project.title}` on the outer `Link`**, not left to the browser's default (concatenating title + summary + tags + year) — that default would still have been readable, but "the accessible name is the project title" in the AC reads as a direct requirement, not a preference, so it's pinned rather than trusted. **Tags are plain non-interactive `<li>` chips, not links** — the AC's own warning ("the tags inside it cannot also be links") is structural: a `<Link>` cannot contain another `<a>`, so nothing about a tag being a real filter link was ever on the table for this ticket; `Badge` (PORT-022) replaces the inline chip classes later, marked with the same `PORT-022 replaces this` convention `not-found.tsx`/`error.tsx` used for `Button`. **Two new tokens shipped with this ticket**, both in `globals.css`: `--shadow-card` / `--shadow-card-lift` (raw `--card-shadow*` vars in `:root`/`.dark`, feeding `@theme inline` under different names — same self-reference trap as `--font-sans`/`--font-inter`) settle the dark-shadow gap deferred from PORT-003, and `--aspect-thumbnail: 16 / 10` turns the already-decided 16:10 thumbnail ratio into a real CSS box so `next/image fill` + `object-cover` absorbs a future off-ratio replacement (PORT-057) without a layout shift. **Card itself carries no shadow at rest** — elevation stays border-only exactly as this table already said before either component existed; the shadow tokens are used only by `ProjectCard`'s hover/focus state (`group-hover:`/`group-focus-visible:`, so a keyboard user gets the same affordance a mouse user gets, not just `:hover`). **The `Button`-on-`Card` focus-ring-offset trap flagged in ui-rules §6 did not fire here** — `ProjectCard` has no inner `Button`, only the outer whole-card `Link`, whose ring sits on `ground` (the page background immediately outside the card), which is the correct color already. The trap is still open for whichever ticket first puts a `Button` inside a `Card`. Verified via a throwaway `/dev-probe-021` route (deleted after) rendering all four real projects — 80 scripted Playwright assertions across 1440/1024/768/375 × light/dark (grid columns, no overflow, accessible name, no nested `<a>`, thumbnail aspect ratio, hover border/shadow change, grid column count), plus a manual read of the full focus-ring `box-shadow` value (not just "not none") and screenshots confirming a real two-layer ring — `ground`-colored offset ring, `fern` ring beyond it — in both themes. |
| 2026-08-22 | `Button` | Built (PORT-020), first primitive of Sprint 2, and the three inlined copies of its classes in `not-found.tsx` (one) and `error.tsx` (two) were swept in the same ticket. **Polymorphism is a discriminated union on `href`, not `asChild`**: `href?: undefined` on the button branch versus `href: string` on the link branch, which narrows cleanly and forwards every native prop of whichever element it becomes — no Slot, no `cloneElement`, no extra dependency. **TypeScript's excess-property check on a union does not do what you would hope**: it accepts a prop present on *any* member, so `<Button href="/" disabled>` compiled and would have shipped a link that looks dead and still navigates. Blocked with `type?: never; disabled?: never` on the link branch, proven with `@ts-expect-error` probes. The mirror case is deliberately left open — `<Button prefetch={false}>` with no `href` still compiles and React warns at runtime — because closing it means listing every `next/link` prop as `never` and re-listing it whenever Next adds one. **`type="button"` is defaulted before the prop spread**, so a caller can override it with `type="submit"` (PORT-041 will) while a bare `<Button>` inside a `<form>` never submits it by accident. **Icons are children, not props** — `gap-2` plus `[&_svg]:size-4` in the base handles leading and trailing alignment with no extra API; measured at exactly 16×16 and vertically centred. `cursor-pointer` is in the base because **Tailwind v4 changed the default button cursor to `default`** — without it every `<button>` loses its pointer. Three values were settled here that no doc specified: `ghost` is `text-muted` at rest (matching `NavLinks` idle, keeping three genuinely distinct tiers, and measuring 5.35:1 light / 6.97:1 dark), the press state is a uniform `active:brightness-95` (there is no `-active` colour token and inventing three was worse than one filter), and `sm` is 36px to match the height `ThemeToggle` and the burger already use. |
| 2026-08-22 | `Header`, `Footer` | Rewired to `src/content/site.ts` (PORT-011). No class changed — this is a data-source swap. `Header` lost its `NAV` array and the name from its markup; `Footer` lost `SOCIALS` and gained LinkedIn. Both import `@/content/site` **directly** rather than through `lib/`, because `site` is a config singleton with nothing to query (rationale in progress.md decisions, carve-out written into content-model §4). **`Footer` now splits accessible name from visible text**: `site.socials[].label` goes on `aria-label` ("GitHub profile") while the visible word comes from a `PLATFORM_TEXT: Record<SocialLink["platform"], string>` map in the component — so adding a platform to the union in `types.ts` fails the build here instead of rendering `undefined`. That split carries a rule: **WCAG 2.5.3 Label in Name** — the accessible name must contain the visible text, or voice control stops matching "click GitHub". Presentation words stay in the component, not in content. `NavLinks` and `MobileMenu` were not touched; taking links as a prop is what made the swap free. |
| 2026-08-21 | `MobileMenu`, `NavLinks` | `MobileMenu` built (PORT-007) — **Sprint 0 complete.** Sheet and scrim are **portaled to `<body>`**, not rendered in the header: the header carries `backdrop-blur-md`, and an element with a backdrop-filter becomes the containing block for its `position: fixed` descendants, so a `fixed inset-0` scrim inside it sizes to the 64px header box. That portal then broke the focus trap — the sheet sits after the footer in the DOM while appearing under the burger, so a trap that only intercepted the two ends of its cycle let forward-Tab walk the burger into the footer. Every Tab is now `preventDefault`ed and stepped by modulo. **No `role="dialog"`/`aria-modal`**: it would hide the burger — the labelled close control — from assistive tech; the ticket specifies a disclosure, so it is one. Background content is therefore still reachable by a virtual cursor; `inert` on `main`/`footer` is the fix, raised for PORT-052. Sheet is **not** `lg:hidden` — CSS-hiding an open sheet leaves an invisible element holding the scroll lock; `matchMedia` closing it is the single mechanism. `NavLinks` gained `orientation` and `onNavigate` rather than a second link list being written; `onNavigate` is a click callback, not a pathname effect, because tapping the current page's link never changes the pathname. |
| 2026-08-21 | `not-found`, `error` (route-level pages) | Built (PORT-006), together with six route stubs. Neither is a registry component — they are pages — but both own UI, so their states are now `built` above. **Both inline the designed `Button` classes** rather than importing one: `Button` is PORT-020 and building it early would be building ahead. Each inlined spot carries a `PORT-020 replaces this` comment; that ticket must sweep them. `error.tsx` is a Client Component (Next requires it — `reset` is a callback) and therefore **cannot export `metadata`**; `not-found.tsx` can and does. `error.digest` is rendered only when present, because it exists in production builds and not in dev. Route stubs carry a mono eyebrow naming the ticket that fills them, so an unfinished page is obvious on sight. |
| 2026-08-20 | `Header`, `NavLinks`, `Footer`, `SkipLink`, `ThemeToggle` | Built (PORT-005). Nav array and footer socials are hardcoded in `Header`/`Footer` until PORT-011. **Nav collapses at `lg:` (1000), not `md:` (760)** — seven items measure ~720px against a 760px header, inside the margin of error, and ui-rules §4 reserves an unbuilt header CTA slot; §4 corrected. `ThemeToggle` uses `useSyncExternalStore` for the hydration gate because Next 16 lint rejects the `useState`+`useEffect` pattern the impl-guide prescribes. Footer socials are text links, not brand icons — `lucide-react` v1 removed `Github`/`Linkedin` entirely. `MobileMenu` stays `designed` (PORT-007). |
| 2026-08-19 | `Container`, `Section`, `Prose` | Built (PORT-004). Prototype classes replaced with the real token utilities. **`Section` now wraps its children in a `Container`** — the impl-guide page example (`<Section heading="Selected work">` with no wrapper) already assumed this; ui-rules §4's diagram did not, and was corrected. `SectionHead` stays `designed` — Section's plain heading slot covers the common case, and the eyebrow + gradient rule treatment lands when a page needs it. |
| 2026-08-18 | All | Populated from the approved design prototype. Every row `designed` — proven visually, not yet built in React. |
| 2026-08-17 | — | Registry rewritten for the Next.js portfolio scope. |
